{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", 
    "paths": {
      "@/*": ["./*"],                // already hai
      "@config/*": ["app/*"]         // 🔥 yeh maine add kiya
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
}